#include <bits/stdc++.h>
using namespace std;

void explainSet() {
    unordered_set <int> us;
    // ? it is not sorted , but it is unique
    // lower_bound and upper_bound functions
    // do not work as the rest of the functions
    // are same as other , it does not stores in any particular order
    // it has a better complexity than set in most of the cases
    // except some when collison happens
}